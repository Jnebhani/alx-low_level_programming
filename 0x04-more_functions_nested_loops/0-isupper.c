#include"main.h"

/**
 * _isupper - checks for upprercase character
 * @c: The character to be checked
 *
 * Return on success 1 if the character is uppercase, otherwise 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
{
	return (isupper);
}
else
{
	return (0);
}
}
