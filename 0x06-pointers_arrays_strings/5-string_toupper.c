#include "main.h"
#include <stdio.h>

/**
 * string_toupper - changes lowercase to uppercase
 * @n: pointer
 * Return: char
 */
char *string_toupper(char *)
{
	int i;

	i = 0;
	while (n[i] != '\0')
	{
i		if (n[i] >= 'a' && n[i] <= 'z')
			n[i] = [i];
		i++;
	}
	return (n);
}
